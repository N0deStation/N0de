# Site assets

Drop files here and reference them from `docs/index.html` with relative paths (`assets/...`).

| File | Used by | Notes |
|---|---|---|
| `report.pdf` | Hero + Resources | Final report (TODO) |
| `poster.pdf` | Resources | Poster (TODO) |
| `slides.pdf` | Resources | Final presentation slides (TODO) |
| `architecture.svg` | How it works | Placeholder block diagram; replace with the real one |
| `logo.svg` | Nav / favicon | Generated placeholder mark |
| `demo.gif`, `screenshot-*.jpg` | Demo gallery | Add entries to the `GALLERY` array in `index.html` |
| `team/<name>.jpg` | Team cards | Add `photo` paths to the `TEAM` array in `index.html` |

Keep images under ~500 KB each so the page stays fast.
